//jeito de declarar a arrow function
const doubleVelocity = (velocity) => {
  return velocity * 2
}

console.log(doubleVelocity(60))

//jeito simples de se declarar a arrow function
// const doubleVelocity = (velocity) => velocity * 2

//jeito mais simples de se declarar a arrow function (apenas com um parâmetro)
// const doubleVelocity = velocity => velocity * 2